platform :ios, '8.0'

use_frameworks!

link_with 'Extensions'

target :FilterIt, :exclusive => true do
    link_with 'FilterIt'
    pod 'GPUImage'
end

target :Meetups, :exclusive => true do
    link_with 'Meetups'
    pod 'Alamofire'
    pod 'SwiftyJSON'
end
